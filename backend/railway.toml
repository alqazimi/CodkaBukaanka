# Prefer repo-root railway.toml (Root Directory empty). Use this file only if Root Directory = backend.
[build]
builder = "NIXPACKS"

[deploy]
preDeployCommand = "npm run db:deploy"
startCommand = "npm run start"
healthcheckPath = "/health"
