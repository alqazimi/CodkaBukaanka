# App database user (used by Prisma at runtime)
DATABASE_URL="postgresql://app_user:StrongPassword123!@localhost:5432/diiwaanka_bukaanka"

# One-time setup only — your local PostgreSQL superuser password
POSTGRES_PASSWORD="your-postgres-password"

PORT=4000
JWT_SECRET="GYr6Uk2b6TPhs9Q7kaGoYYWDNq06CnSoQrx57kUAYzY="
FRONTEND_URL="http://localhost:3000"
# Optional comma-separated allowlist for production CORS
# FRONTEND_URLS="https://your-frontend.com,https://admin.your-frontend.com"
# Enforce admin 2FA at login (auto-enabled in production)
ENFORCE_ADMIN_TOTP=true
# Optional captcha provider verification endpoint + secret
# CAPTCHA_VERIFY_URL="https://challenges.cloudflare.com/turnstile/v0/siteverify"
# CAPTCHA_SECRET=""

ADMIN_EMAIL="admin@diiwaankabukaanka.org"
ADMIN_PASSWORD="AdminChangeMe123!"

CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Optional: Redis for distributed rate limiting (production)
# REDIS_URL="redis://localhost:6379"

# Set true when behind reverse proxy (nginx, Cloudflare, Railway)
# TRUST_PROXY=true
